#video__container
    video#me(autoplay,playsinline)
    span(class="material-icons" , id="mute" style="color:white" onClick="onClickMute()") volume_up
    span(class="material-icons", id="camera" style="color:white" onClick="onClickCam()") videocam_on
    select(id="change" onChange="hanldeCamChange()")