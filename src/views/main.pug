include common/modal.pug

div.main
    #chat__container
        include sidebar/main.pug
        include chat/main.pug
    div(class="fab" onClick="handleModal()")="+"
    +modalMixin()
        .add-room-container
            header Add Room
            .body
                input(type="text" placeholder="Room name" id="room_name")
                input(type="url" placeholder="Room image" id="room_image")
                button(id="room_submit") add