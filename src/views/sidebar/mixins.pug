mixin inputbox(type, value, id, onClick)
    .item-container
               span=type
               .input-container
                    input(type="text" disabled="true" value=value id=id)
                    span(class="material-icons" onClick=onClick) edit